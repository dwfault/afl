afl-ascii

If input is in ascii range, the deterministic mutation in would keep mutated samples in ascii rage too.

Current approach relies on keeping MSB always zero, thus the mutated bytes could be always below 0x7F.